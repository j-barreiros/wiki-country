import styled from 'styled-components';

export const StyledCountryIngoBox = styled.article`
  width: 300px;
  background: hsl(0, 0%, 52%);
  border-radius: 3px;
  overflow: hidden;

  img {
    width: 100%;
    height: auto;
  }
`